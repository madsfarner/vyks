declare var require: any

var React = require('react');

export default class Kategorier extends React.Component {
    render() {
        return (
            <h1> Kategorier </h1>

        );
    }
}